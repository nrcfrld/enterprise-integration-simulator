# Implementation Checklist — Enterprise Integration Simulator

Audit date: 2026-09-02  
Scope: Marketplace Simulator, PRD v0.2 as superseded for orders by **Order Lifecycle & API Brief**

Status summary: **83 COMPLETE · 0 PARTIAL · 0 NOT_IMPLEMENTED**

The status below is based on executable evidence, not merely the presence of a route or source file. `Testcontainers` means isolated PostgreSQL and Redis containers; `Compose E2E` means the independently running Docker stack.

| # | Requirement | Status | Implementation location | Test / evidence |
|---:|---|---|---|---|
| 1 | External developers can integrate through public REST API + documentation without source/database access. | COMPLETE | `openapi/openapi.yaml`, `docs/marketplace/*`, `examples/*` | Compose E2E issues signed requests only through HTTP; Admin Developer Portal, Swagger, and OpenAPI loaded in browser. |
| 2 | Marketplace is an independent, isolated application in a monorepo. | COMPLETE | `apps/marketplace`, `packages/dummy-generator`, `go.work` | Dependency and filesystem audit: Marketplace has no SAP/Shipping package or database dependency. Future simulators remain explicitly out of initial scope. |
| 3 | Persistent business state uses a dedicated PostgreSQL database and required entities/JSON payloads. | COMPLETE | `migrations/001_initial.sql` | Compose starts `marketplace-db`; Testcontainers exercises shops, products, orders, events, deliveries, JSONB snapshots. |
| 4 | Redis is used for rate limit and background/retry infrastructure, not primary business data. | COMPLETE | `internal/ratelimit/fixed_window.go`, `cmd/worker/main.go` | Testcontainers rate-limit and worker suites use real Redis. |
| 5 | Go, Gin, pgx, sqlc, and Goose are used with SQL-first access. | COMPLETE | `go.mod`, `internal/server`, `internal/store`, `migrations`, `internal/platform/migrate.go` | `go generate ./internal/api/openapi ./internal/store` and backend tests pass. |
| 6 | Asynchronous jobs use Asynq. | COMPLETE | `cmd/worker/main.go` | Worker starts in Compose; Testcontainers proves outbox publication, enqueue, delivery, and retry. |
| 7 | Admin control panel provides Dashboard, Shops, Products, Orders, Order Detail, Credentials, Webhooks, Deliveries, and Scenarios. | COMPLETE | `admin/src/features/control-plane/ControlPlaneApp.tsx` | Authenticated browser verification rendered every page with live seeded data and no console errors. |
| 8 | API contract is specification-first OpenAPI with Swagger and integration documentation. | COMPLETE | `openapi/openapi.yaml`, `internal/api/openapi/client.gen.go`, `docs/marketplace/*` | Served `/openapi.yaml` and `/swagger/index.html`; generator and `TestSwaggerUIIsServedFromTheCommittedOpenAPISpec` pass. |
| 9 | Structured logging and operational metrics are available. | COMPLETE | `internal/observability`, `cmd/api/main.go`, `cmd/worker/main.go` | Compose worker logs structured delivery records; `/metrics`, request IDs, `/health`, and `/ready` implemented. |
| 10 | Go tests plus real PostgreSQL/Redis Testcontainers cover priority integration behavior. | COMPLETE | `integration/*_test.go`, `cmd/worker/testcontainers_test.go` | Full race-enabled Testcontainers suite passes. |
| 11 | Generic dummy data is separated from Marketplace domain data and uses gofakeit. | COMPLETE | `packages/dummy-generator`, `internal/products/seed.go` | `packages/dummy-generator`: `go test ./...` passes; reset produces 100 products / 50 orders. |
| 12 | `docker compose up --build` starts API, worker, PostgreSQL, Redis, and Admin UI. | COMPLETE | `docker-compose.yml`, Dockerfiles | Final Compose build completed; all five services were Up and PostgreSQL/Redis healthy. |
| 13 | Multiple shops are supported and data is isolated per shop. | COMPLETE | `shops` schema, control API, `internal/store/queries.sql` | `TestContainerResetPermissionAndTransactionalOutbox` and `TestContainerRateLimitAndScenarioIsolation` pass. |
| 14 | Credentials are one-time-secret, encrypted at rest, and revocable. | COMPLETE | `credentials` schema, `platform/crypto.go`, server credential handlers | Browser shows credential creation/revocation controls; Testcontainers checks reset does not leak secrets and public auth checks revoked credentials. |
| 15 | Public requests require client ID, timestamp, HMAC-SHA256 signature, expiry, constant-time comparison, and rate limit before expensive body processing. | COMPLETE | `internal/server/server.go` (`integrationAuth`) | Unit crypto/auth tests and Compose signed-product E2E pass. |
| 16 | Product create/list/detail/partial update/delete support search, filters, sorting, pagination, and product events. | COMPLETE | `internal/server/server.go`, OpenAPI | Compose E2E and Testcontainers cover creation, pagination, idempotent delete/event, and partial PATCH semantics. |
| 17 | Admin supports random and custom customer-order simulation, creating `UNPAID` orders. | COMPLETE | `createSimulatedOrder`, Admin Orders page | Compose lifecycle E2E and browser show new orders in `UNPAID`. |
| 18 | Orders preserve customer/address/item/price snapshots. | COMPLETE | `orders`, `order_items` schema; `createSimulatedOrder` | Custom-order E2E validates customer and item snapshots; browser Order Detail displays them. |
| 19 | Lifecycle transitions are validated by one domain state machine: `UNPAID → PAID → PROCESSING → READY_TO_SHIP → SHIPPED → IN_DELIVERY → DELIVERED → COMPLETED`, with pre-shipping cancellation only. | COMPLETE | `internal/orders/lifecycle.go`, `transitionTx` | Unit transition table plus Testcontainers lifecycle flow reject invalid transitions and complete the valid path. |
| 20 | Order list/detail supports cursor pagination, status/date filters, sorting, snapshots, payment information, shipment information, and timestamps. | COMPLETE | `listOrders`, `getOrder`, OpenAPI `Order`/`Payment` | Signed Testcontainers and Compose lifecycle E2E retrieve `PAID` payment metadata and shipment data. |
| 21 | Fulfillment uses a distinct shipment resource created only from `READY_TO_SHIP`, with order-shipments and shipment-detail retrieval. | COMPLETE | `createShipment`, `listOrderShipments`, `getShipment`, migration `005` | Testcontainers and Compose E2E create `CREATED` shipment, then read both public endpoints. |
| 22 | Every product/order state change writes a durable domain event with unique identity, including new lifecycle event names. | COMPLETE | `internal/events/outbox.go`, `transitionTx`, `domain_events`, `outbox` | Testcontainers asserts complete new event sequence; Compose E2E receives seven lifecycle webhooks. |
| 23 | Signed public webhook register/list/delete endpoints validate event subscriptions and hide stored secrets. | COMPLETE | `createWebhook`, `listWebhooks`, `deleteWebhook` | Testcontainers rejects unsupported events; Compose E2E registers endpoint and receives delivery. |
| 24 | Webhook delivery is asynchronous and does not block business operations. | COMPLETE | transactional outbox + `cmd/worker` loops | Compose E2E receives `order.created` asynchronously after order creation. |
| 25 | Webhooks include type, stable event ID, timestamp, and HMAC signature. | COMPLETE | `worker.handleDelivery`, webhook guide | `TestOrderCreatedWebhookIsSignedAndDelivered` verifies every header and signature. |
| 26 | Every delivery attempt stores request/response/status/duration/attempt history. | COMPLETE | `webhook_deliveries`, `webhook_delivery_attempts`, delivery handlers | Worker Testcontainers asserts two immutable attempt rows; Deliveries UI shows attempts/retry. |
| 27 | Retry schedule is immediate, +30s, +2m, +10m, +30m, then FAILED; manual retry exists. | COMPLETE | `internal/webhooks/retry.go`, worker, `retryDelivery` | Compose E2E verifies the real ~30-second retry; worker suite verifies durable scheduling and attempt exhaustion logic. |
| 28 | Admin exposes webhook delivery logs and details. | COMPLETE | control delivery routes and Admin Deliveries page | Browser displays delivery records, status, attempt count, and Retry/Attempts actions. |
| 29 | Redis-backed configurable quota is 100 requests/minute/credential with documented headers. | COMPLETE | `fixed_window.go`, config, `integrationAuth` | Testcontainers verifies `429` and all rate-limit headers; Compose E2E verifies default limit header. |
| 30 | Public list APIs use opaque cursor pagination. | COMPLETE | `internal/server/pagination.go`, list handlers | Unit cursor tests plus Compose E2E verifies non-duplicated adjacent pages. |
| 31 | API slow response, deterministic/probabilistic 500, timeout, and forced rate-limit scenarios work per shop. | COMPLETE | `scenarios/config.go`, `applyScenario`, control routes | Added Testcontainers scenario test verifies 500, >=100ms slow response, client timeout, and shop isolation/rate limit. |
| 32 | Webhook force-failure scenario records failure and follows retry behavior. | COMPLETE | `worker.handleDelivery`, scenario config | Added worker Testcontainers assertion records a `FAILURE` attempt under forced failure. |
| 33 | Duplicate webhook simulation and manual duplicate preserve the same event ID while creating distinct deliveries. | COMPLETE | worker outbox fan-out; `duplicateEvent` | Compose E2E validates manual duplicate stable ID; added worker test asserts duplicate scenario creates two delayed deliveries. |
| 34 | Delayed webhook scenario and manual delay use background scheduling. | COMPLETE | worker scheduling; `delayEvent` | Compose E2E validates manual delayed event; worker Testcontainers asserts scheduled delay. |
| 35 | Out-of-order delivery can delay `order.paid` while internal state remains valid. | COMPLETE | worker `publishOutbox` | Worker Testcontainers validates `order.paid` delayed at least ten seconds. |
| 36 | Maintenance mode returns 503 for integration API while control plane stays available. | COMPLETE | `isMaintenance`, control maintenance routes | Compose E2E `TestMaintenanceReturnsDocumentedPublicFailure` passes. |
| 37 | Dashboard presents setup state and operational counts. | COMPLETE | `dashboard`, Admin Dashboard | Authenticated browser displays shop/order/failed delivery counts and guided setup state. |
| 38 | Order Detail provides snapshots, payment/shipment, event history, delivery history, new lifecycle actions, shipment actions, and debug controls. | COMPLETE | `orderDetail`, `events`, `deliveriesForOrder`, Admin Order Detail | Runtime browser check shows payment, `Verify payment`, `Ready to ship`, shipment movement controls, and no console errors. |
| 39 | Development seed and per-shop reset create one shop, 100 products, 50 historical orders, active credential, and disabled example webhook without exposing secrets. | COMPLETE | `EnsureDevelopmentSeed`, `resetShopData`, `products/seed.go` | Browser Dashboard confirms 100 products; Testcontainers confirms reset isolation and no secret leakage. |
| 40 | `/health` checks liveness and `/ready` checks PostgreSQL + Redis. | COMPLETE | server routes and `ready` | Final Compose service check and direct endpoint verification. |
| 41 | Security constraints: redaction, encrypted secrets, parameterized SQL, session validation, authorization/isolation, timestamp expiry, and no plaintext re-display. | COMPLETE | `platform/crypto.go`, auth middleware, sqlc/parameterized queries, docs | Unit auth/crypto/config tests; Testcontainers session/permission/secret-leak tests pass. |
| 42 | Developer experience includes guides, OpenAPI/Swagger, request-signing examples, Node/Go examples, and scenario documentation. | COMPLETE | README, `docs/marketplace/*`, `examples/*`, Admin Documentation portal | Authenticated Documentation portal and OpenAPI/Swagger links render; both Go and Node/TypeScript examples made signed HTTP requests and each returned 10 products. |
| 43 | Marketplace v1 Definition of Done flow is executable end-to-end. | COMPLETE | end-to-end system | Existing controls still pass; new Compose E2E verifies full public/admin lifecycle and durable webhook delivery. |

| 44 | Public order statuses exactly match the new brief; obsolete `CREATED`/`CONFIRMED` order statuses are removed. | COMPLETE | `orders/lifecycle.go`, migration `005_order_lifecycle_v1.sql`, OpenAPI | Persistent Compose database successfully migrated old rows (`CREATED → UNPAID`, `CONFIRMED → PAID`); served OpenAPI lists only new order statuses. |
| 45 | Generic has no public order, package, or shipment route; business actions are exposed only through a provider contract. | COMPLETE | `Router`, removed legacy handlers, migration `009_remove_generic_order_profile.sql` | Testcontainers signs `GET /api/v1/orders` and receives 404; OpenAPI route audit has no `/api/v1/orders`, `/packages`, or `/shipments` operations. |
| 46 | Merchant API supports provider-owned transitions and cancellation: Shopee-like `ship-order`/`ready-to-ship`/`cancel`, Tokopedia-like `pack`/`handover`/`cancel`. | COMPLETE | Shopee/Tokopedia route groups and handlers | Full Testcontainers matrix verifies valid and invalid provider transitions plus cancellation behavior. |
| 47 | Payment verification is simulator-only and exposes payment reference/status/timestamp in order detail. | COMPLETE | control `PAY` action, `transitionTx`, `paymentInfo` | Lifecycle E2E verifies PAID order detail contains reference and `paid_at`; public API has no pay action. |
| 48 | Shipment creation is public only for `READY_TO_SHIP`, accepts provider plus `PICKUP`, and starts `CREATED` through Shopee-like or Tokopedia-like routes. | COMPLETE | `createShipmentForOrder`, provider handlers, OpenAPI `ShipmentInput` | Testcontainers creates and progresses provider-specific shipments with generated tracking number. |
| 49 | Simulator controls `SHIPPED`, `IN_DELIVERY`, and `DELIVERED` after shipment creation, then completion. | COMPLETE | `shipmentAction`, `transitionShipment`, `orderAction` | Testcontainers and Compose E2E advance shipment through all three states then complete the order. |
| 50 | Webhook contracts accept and emit `order.created`, `paid`, `processing`, `ready_to_ship`, `shipped`, `in_delivery`, `delivered`, `completed`, and `cancelled`. | COMPLETE | `internal/webhooks/events.go`, `transitionTx`, worker, OpenAPI | Testcontainers asserts event sequence; real Compose worker delivered all seven subscribed post-creation lifecycle events. |
| 51 | Admin UI reflects the lifecycle and preserves the operator boundary. | COMPLETE | `ControlPlaneApp.tsx` | Browser runtime shows UNPAID/payment/shipment state and Verify payment, Process, Ready to ship, Complete, Cancel, and shipment movement controls; console error list empty. |
| 52 | OpenAPI, Developer Portal, README, guides, seeds, worker scenario behavior, and generated bindings reflect the replacement contract. | COMPLETE | `openapi.yaml`, `client.gen.go`, `docs/marketplace/*`, `README.md`, `endpoints.ts`, seed/reset, worker | Generator ran; served OpenAPI and browser Developer Portal show new endpoints/events; production Admin build passes. |
| 53 | New lifecycle works end-to-end without regression in existing product, auth, webhook, retry, scenario, Docker, and documentation behavior. | COMPLETE | integration suites, Compose stack | Go suite, frontend suite, Testcontainers race suite, focused Compose lifecycle E2E, real worker retry logs, readiness/OpenAPI checks all pass. |
| 54 | Admin Shipments is a separate fulfillment workspace: it lists only the selected shop’s shipments, shows linked-order/tracking fields, and permits only the next valid fulfillment transition. Shipment creation remains public API-only. | COMPLETE | `shopShipments`, `shipmentDetail`, `shipmentAction`, `ControlPlaneApp.tsx`, `navigation.ts` | Focused Testcontainers lifecycle test verifies list pagination payload, detail lookup, cross-shop denial, timestamp/state updates, and invalid transition rejection; frontend typecheck/lint/unit/build pass. Compose/browser E2E intentionally not run for this update. |
| 55 | Provider profile architecture supports only `SHOPEE_LIKE` and `TOKOPEDIA_LIKE`; existing Generic shops migrate safely to Shopee-like. | COMPLETE | migration `009_remove_generic_order_profile.sql`, `orders/provider.go`, shop create form/API | Full Testcontainers migrations apply through v9; backend/frontend regression pass. |
| 56 | Shopee-like lifecycle applies different transition and actor rules rather than renamed generic statuses. | COMPLETE | `orders/provider.go`, `cancelResponse` | `TestContainerShopeeLikeCancellationMatrix` verifies customer/seller allow/deny matrix; worker test covers SYSTEM cases. |
| 57 | Payment supports expiry, success/failure, and automatic cancellation. | COMPLETE | order payment columns, `failPayment`, worker `enforceOrderDeadlines`, order detail UI | Worker Testcontainers proves expiry state/cancellation/event; API exposes operations detail. |
| 58 | Seller SLA warns or auto-cancels overdue paid/processing Shopee-like orders. | COMPLETE | `seller_deadline_at`, `transitionTx`, worker `enforceOrderDeadlines`, order detail UI | Worker Testcontainers proves SLA cancellation/event; deadline is returned in order operations. |
| 59 | Order, Package, and Shipment are separate fulfillment concepts. | COMPLETE | `packages`, `package_items`, `shipments.package_id`, public/control package endpoints, Admin Packages | Testcontainers proves partial allocation, over-allocation rejection, and multi-package final-delivery semantics. |
| 60 | Cancellation is based on lifecycle position, actor, and reason. | COMPLETE | `CanCancel`, `ValidCancellationReason`, `cancelResponse`, README | Matrix integration test plus documented reason taxonomy. |
| 61 | SHOPEE_LIKE has a separate public API contract with provider terminology, route prefix, filters, payloads, and response envelope. | COMPLETE | `internal/server/server.go` (`/api/shopee/v1`, `shopeeListOrders`, `shopeeGetOrder`), OpenAPI | `TestContainerShopeeLikePublicContract` proves distinct path, `page_no`/`page_size`, `order_sn`, envelope, and profile isolation. |
| 62 | SHOPEE_LIKE uses a different partner-style signing contract; shared HMAC remains only for catalogue, warehouse, and webhook resources. | COMPLETE | `shopeeIntegrationAuth`, integration guide | Testcontainers proves Shopee headers/signature succeed and shared-HMAC headers are rejected at `/api/shopee/v1`. |
| 63 | SHOPEE_LIKE webhooks map canonical events and payloads to provider event names and signing headers. | COMPLETE | `internal/webhooks/provider.go`, worker `handleDelivery`, Shopee webhook handlers | `TestContainerWorkerDeliversShopeeLikeWebhookContract` verifies transformed body, `X-Shopee-*` headers, and signature presence; public registration/list test passes. |
| 64 | SHOPEE_LIKE pagination, error codes, invalid-state result, and rate-limit behavior are provider-specific. | COMPLETE | `shopeeListOrders`, `shopeeError`, `setShopeeRateLimitHeaders`, OpenAPI | Testcontainers proves `error_param`, `error_invalid_state`, page-number result, and `X-Shopee-Api-Call-Limit`; shared resource contract regression still passes. |
| 65 | An exhaustive SHOPEE_LIKE integration matrix covers happy/seller flow, payment and SLA expiry, customer/seller/system cancellation, invalid states, package creation, shipment delivery, and webhook delivery. | COMPLETE | `integration/testcontainers_server_test.go`, `cmd/worker/testcontainers_test.go` | Combined Testcontainers matrix: `TestContainerShopeeLikePublicContract`, `TestContainerShopeeLikeCancellationMatrix`, `TestContainerPackageSupportsPartialItemAllocation`, `TestContainerWorkerEnforcesPaymentExpiryAndSellerSLA`, and `TestContainerWorkerDeliversShopeeLikeWebhookContract`. |
| 66 | `TOKOPEDIA_LIKE` models the combined Tokopedia & Shop / TikTok Shop provider boundary rather than a retired standalone contract. | COMPLETE | `orders/provider.go`, `internal/tokopedia/contract.go`, shop form/API, README | `TestContainerTokopediaLikeRTSAndInventoryReservation` creates and exercises an isolated `TOKOPEDIA_LIKE` shop; Admin typecheck/lint/test/build pass. |
| 67 | `TOKOPEDIA_LIKE` exposes a provider-specific public API, external status mapping, opaque page token, error envelope, and rate-limit headers. | COMPLETE | `/api/tokopedia/v202309` handlers, `internal/tokopedia/contract.go`, `openapi/openapi.yaml` | Testcontainers verifies `pack → AWAITING_SHIPMENT`, `handover → AWAITING_COLLECTION`, signed order search, and the `{code,message,request_id,data}` contract; generated OpenAPI bindings include the new operations. |
| 68 | `TOKOPEDIA_LIKE` has its own app-key/query signing plus access-token authentication; Generic and Shopee signing remain unaffected. | COMPLETE | `tokopediaIntegrationAuth`, `tokopedia.Sign`, credential access-token encryption | Testcontainers issues real HMAC-signed requests with `app_key`, `timestamp`, `sign`, and `x-tts-access-token`; full Go regression suite passes. |
| 69 | Inventory reservation prevents overselling and is safely committed or released for cancellation, payment failure, and deadline expiry. | COMPLETE | migration `007_provider_rts_inventory.sql`, `internal/inventory/reservation.go`, create/cancel/payment/worker paths | Concurrent Testcontainers orders for stock=1 create exactly one order; permitted cancellation restores stock. Existing payment-expiry/SLA worker coverage remains green. |
| 70 | Delivery failure and return-to-sender are explicit shipment/package states; the order becomes `RETURNED` only after all shipments return. | COMPLETE | migration `007`, `transitionShipment`, Admin Shipment Detail, public shipment schemas/docs | Testcontainers drives `IN_DELIVERY → DELIVERY_FAILED → RETURNING → RETURNED`, persists failure reason/timestamps, and verifies the linked order transition atomically. Worker Testcontainers verifies the numeric provider-specific webhook body and `Authorization` signature. |
| 71 | Every shop has one or more warehouse fulfillment origins with code, name, active status, JSON address, priority, and timestamps. Existing shops are safely backfilled with `WH-DEFAULT`. | COMPLETE | `migrations/008_warehouse_fulfillment.sql`, `inventory.EnsureDefaultWarehouse`, `createShop` | `TestContainerWarehouseMigrationBackfillsExistingInventory` migrates a populated v7 database to v8 and proves order/package/reservation warehouse backfill. |
| 72 | Stock is represented at warehouse/product level as physical on-hand, reserved, and available quantity while legacy `products.stock` remains the aggregate available compatibility field. | COMPLETE | `warehouse_inventory`, `updateWarehouseInventory`, `updateProduct` | Warehouse Testcontainers flow asserts 3/2/1 reserve state, 3/0/3 release state, and 1/0/1 physical fulfillment state. |
| 73 | New orders allocate atomically to the highest-priority active warehouse that can fulfill every requested item; allocation records the order origin and prevents overselling. | COMPLETE | `inventory.Allocate`, `createSimulatedOrder`, order `fulfillment_warehouse_id` | `TestContainerWarehouseAllocationReservationAndFulfillment` creates default + priority warehouses and proves the eligible priority origin is selected. Existing concurrent-reservation race test remains green. |
| 74 | Permitted cancellation, payment expiry, payment failure, and seller-SLA cancellation release warehouse reservations exactly once. | COMPLETE | `inventory.Release`, `transitionTx`, `cmd/worker/main.go` | Public cancellation flow verifies reserve release; `TestContainerWorkerEnforcesPaymentExpiryAndSellerSLA` verifies expired payment releases warehouse reserve and restores aggregate availability. |
| 75 | Shipping a package atomically consumes physical warehouse stock and removes its reservation; order and shipment transitions remain atomic. | COMPLETE | `inventory.FulfillPackage`, `transitionShipment` | Warehouse integration test marks a shipment `SHIPPED` and verifies physical stock/reserved quantities in the same tested flow; lifecycle matrix remains green. |
| 76 | The domain models `Order → Package → Warehouse`; v1 assigns one warehouse per order/package while schema supports future package-level distribution. | COMPLETE | `orders.fulfillment_warehouse_id`, `packages.warehouse_id`, package/shipment handlers | Integration test reads warehouse reference from order, package, and shipment; partial-package/provider regression tests pass. |
| 77 | Admin Control Plane has an independently routed **Warehouses** workspace with create, list, detail, inventory display, and order/package fulfillment-origin visibility. | COMPLETE | `admin/src/app/navigation.ts`, `ControlPlaneApp.tsx`, control warehouse handlers | `bun run typecheck`, `bun run lint`, `bun test`, and production build pass; navigation test asserts `/warehouses` and distinct active state. |
| 78 | Generic public warehouse list/detail APIs are authenticated, shop-isolated, rate-limited, and specified in OpenAPI; provider-specific API contracts are unchanged. | COMPLETE | `GET /api/v1/warehouses`, `GET /api/v1/warehouses/:id`, `openapi/openapi.yaml` | Warehouse Testcontainers flow signs both APIs, validates inventory detail, and proves another shop receives `404`; `go generate ./internal/api/openapi` succeeds. |
| 79 | Developer and operator documentation explains warehouse selection, reservation/release, physical deduction, public reads, and control-plane inventory management. | COMPLETE | `README.md`, `docs/marketplace/integration-guide.md`, `docs/marketplace/operations-guide.md`, Developer Portal Warehouse reference | Documentation and generated OpenAPI are included in the successful frontend/backend build checks. |
| 80 | Warehouse migration and all existing backend/frontend/provider workflows have regression coverage without Compose/browser E2E. | COMPLETE | `integration/testcontainers_server_test.go`, `cmd/worker/testcontainers_test.go` | `go test ./...`, dummy-generator tests, frontend checks, and `go test -count=1 -race -tags=testcontainers ./integration ./cmd/worker` all pass; Compose/browser E2E was intentionally excluded by user constraint. |
| 81 | Generic order-provider schema and behavior are removed; the only supported order profiles are Shopee-like and Tokopedia-like. | COMPLETE | migrations `009`, `orders/provider.go`, `server.Router`, OpenAPI, Developer Portal, docs | Migration v9 converts existing Generic shops to Shopee-like before tightening the constraint. Full Testcontainers race matrix, Go suite, and Admin typecheck/lint/unit/build pass. |
| 82 | Public product catalogue contracts are provider-specific; no Generic public product API remains. | COMPLETE | Shopee/Tokopedia product handlers, `Router`, OpenAPI, Developer Portal, provider client examples, README/integration guide | `TestContainerProviderProductContracts` verifies provider-specific fields/envelopes/signing and asserts `/api/v1/products` returns 404. |
| 83 | Developer Portal and API Request Simulator cover every current public shared, Shopee-like, and Tokopedia-like operation with contract-aware signing, executable defaults, success/error examples, and workflow guidance. | COMPLETE | `admin/src/features/developer-portal/*`, `docs/marketplace/integration-guide.md` | Frontend unit tests assert all 25 public operations are catalogued; typecheck, lint, tests, and production build pass. |

## Fixes made during this audit

1. **Admin production bundle crash** — `Prism is not defined` made `http://localhost:5173` blank. Removed the unsafe forced Prism vendor chunk in `apps/marketplace/admin/vite.config.ts`; the rebuilt Docker Admin UI now renders and has no browser console error.
2. **Destructive PATCH behavior** — an omitted `price` or `stock` was interpreted as zero. `productPatchInput` now uses pointers, preserving omitted fields while accepting explicit zero values. OpenAPI now declares `ProductPatchInput`, generated client bindings were refreshed, and both Testcontainers and Compose E2E regression coverage were added.
